# 功能点拆分

> 基于：[需求分析文档](./集团经济分析自动化需求分析.md)、[结构分析文档](./经济分析表结构分析.md)、[实现方案](./实现方案.md)
> 日期：2026-07-13
> 用途：逐点实现、逐点验证，每个功能点完成后即可确认该点正确

---

## 依赖关系总览

```
FP-0.1 项目骨架
  └→ FP-0.2 配置加载 ──→ FP-0.3 日志模块
        ├→ FP-1.1 模板加载
        │     └→ FP-1.2 模板一致性校验
        ├→ FP-1.3 权属文件识别
        │
        ├→ FP-2.1 B列匹配器 ──────────────┐
        │     ├→ FP-2.2 报表1写入          │
        │     ├→ FP-2.3 报表2写入          │
        │     ├→ FP-2.4 报表4写入          ├── 固定行数报表（按行号映射 + B列匹配）
        │     ├→ FP-2.5 报表6写入          │
        │     └→ FP-2.6 报表3写入（特殊排列）┘
        │
        ├→ FP-3.1 合并单元格保护（贯穿写入过程）
        │
        ├→ FP-3.2 报表5双栏追加
        ├→ FP-3.3 报表7多子表园区匹配
        ├→ FP-3.4 报表8明细追加
        │
        ├→ FP-4.1 合计值校验
        ├→ FP-4.2 空值/异常值检测
        │
        └→ FP-5.1 汇总文件保存
              └→ FP-5.2 处理日志输出
```

---

## 第零层：基础设施

### FP-0.1 项目骨架搭建

| 项 | 内容 |
|---|---|
| **描述** | 创建项目目录结构，安装 openpyxl、pyyaml 依赖 |
| **输入** | 无 |
| **输出** | `project/` 目录结构，`pip install` 完成 |
| **验证** | `python -c "import openpyxl; import yaml; print('OK')"` 无报错 |

```
project/
├── config.yaml
├── main.py
├── engine/
│   ├── __init__.py
│   ├── config_loader.py
│   ├── reader.py
│   ├── matcher.py
│   ├── writer.py
│   ├── report5_handler.py
│   ├── report7_handler.py
│   ├── report8_handler.py
│   └── validator.py
├── logger.py
└── output/
```

### FP-0.2 配置加载

| 项 | 内容 |
|---|---|
| **描述** | 实现 `config_loader.py`，解析 `config.yaml`，返回结构化配置对象 |
| **输入** | `config.yaml` 文件 |
| **输出** | 包含 `quarter`、`ownership_files`、`reports`、`runtime` 的字典/对象 |
| **依赖** | FP-0.1 |
| **验证** | 打印加载后的配置内容，确认所有字段正确解析（季度参数、权属映射、行号映射、列范围） |

**需加载的配置项**：
- `quarter.label` / `quarter.end_date` — 季度参数
- `ownership_files` — 文件名→单位列表映射
- `reports.report1~8` — 各报表的 sheet_name、行号映射、列范围
- `runtime` — 模板路径、数据目录、输出目录、reports_to_run

### FP-0.3 日志模块

| 项 | 内容 |
|---|---|
| **描述** | 实现 `logger.py`，封装 Python logging，同时输出到控制台和文件 |
| **输入** | 日志级别配置（来自 config.runtime.log_level） |
| **输出** | 格式化日志（时间戳 + 级别 + 模块 + 消息），写入 `output/` 目录 |
| **依赖** | FP-0.2 |
| **验证** | 运行测试脚本，确认控制台和文件均输出预期日志 |

---

## 第一层：模板与数据源读取

### FP-1.1 模板加载

| 项 | 内容 |
|---|---|
| **描述** | 实现 `reader.py` 中的 `load_template()`，用 openpyxl 加载汇总表模板，返回 workbook 对象 |
| **输入** | 模板文件路径（来自 `config.runtime.template_path`） |
| **输出** | openpyxl Workbook 对象，包含8个工作表 |
| **依赖** | FP-0.2 |
| **验证** | 打印所有工作表名称，确认8个报表（报表1~8）均存在；打印每个报表的合并单元格数量 |

### FP-1.2 模板一致性校验

| 项 | 内容 |
|---|---|
| **描述** | 实现 `reader.py` 中的 `validate_template()`，校验模板中 `row_mapping` 声明位置的实际 B 列值是否与配置一致 |
| **输入** | 模板 Workbook + 配置中的 `row_mapping` |
| **输出** | 校验通过（无异常）或抛出 `TemplateMismatchError` |
| **依赖** | FP-1.1 |
| **验证** | 用Q2模板 + 正确的 row_mapping 应通过；故意改错一个行号映射应报错 |

**校验范围**：报表1/2/3/4/6/7（各子表）的 row_mapping 中所有行，对比 B 列（报表7为 A 列园区名称）实际值与配置预期值。

### FP-1.3 权属文件识别与加载

| 项 | 内容 |
|---|---|
| **描述** | 扫描 `data_dir` 目录，匹配文件名到 `ownership_files` 配置，加载每个权属文件的 workbook |
| **输入** | 数据目录路径 + `ownership_files` 配置 |
| **输出** | `{文件名: {"workbook": wb, "units": ["中侨集团", "侨乡文体"]}}` 的字典 |
| **依赖** | FP-0.2 |
| **验证** | 打印识别到的权属文件列表及对应单位名称，确认6个文件全部匹配 |

**匹配规则**：精确匹配文件名（不含路径），如 `中侨集团.xlsx`。文件名不匹配或文件缺失时输出告警。

---

## 第二层：核心匹配与写入（固定行数报表）

### FP-2.1 B列单位名称匹配器

| 项 | 内容 |
|---|---|
| **描述** | 实现 `matcher.py` 中的 `find_unit_row()`，在权属文件的指定工作表中，按 B 列查找单位名称，返回其行号 |
| **输入** | 权属文件 worksheet + 单位名称（如 "中侨集团"） |
| **输出** | 该单位在 B 列对应的行号（int），未找到返回 None |
| **依赖** | FP-1.3 |
| **验证** | 用 `中侨集团.xlsx` 的报表1工作表，查找 "中侨集团" → 应返回正确行号；查找 "集团本部" → 应返回 None |

### FP-2.2 报表1写入（资产总体情况表）

| 项 | 内容 |
|---|---|
| **描述** | 实现 `writer.py` 中的 `write_report_fixed()`，对报表1执行：遍历 row_mapping → B列匹配 → 逐列写入数值 |
| **输入** | 模板 worksheet（报表1） + 所有已加载的权属文件数据 + 报表1配置 |
| **输出** | 模板 worksheet 中数据区（C7~AJ16）被填入各权属的数值 |
| **依赖** | FP-2.1 |
| **验证** | 运行后打开汇总表，对比报表1中某个权属（如商业集团）的数值与 `商业集团.xlsx` 对应行是否一致 |

**写入规则**：

- 只写入数值单元格（通过 `isinstance(value, (int, float))` 过滤），公式列和文字列不写
- 从 `data_start_col` 到 `data_end_col` 逐列写入

### FP-2.3 报表2写入（按建筑类型区分总资产）

| 项 | 内容 |
|---|---|
| **描述** | 同 FP-2.2，应用于报表2（数据区 C4~T13，total_row=14） |
| **输入** | 模板 worksheet（报表2） + 权属数据 + 报表2配置 |
| **输出** | 数据区数值被填入 |
| **依赖** | FP-2.1 |
| **验证** | 对比汇总表与权属源文件的数值一致性 |

### FP-2.4 报表4写入（各企业出租率）

| 项 | 内容 |
|---|---|
| **描述** | 同 FP-2.2，应用于报表4（数据区 C4~K13，total_row=14） |
| **输入** | 模板 worksheet（报表4） + 权属数据 + 报表4配置 |
| **输出** | 数据区数值被填入（出租率公式列 H~J 不写，由公式自动计算） |
| **依赖** | FP-2.1 |
| **验证** | 确认数值正确，且出租率公式自动计算正确 |

### FP-2.5 报表6写入（国资委经济运行情况表）

| 项 | 内容 |
|---|---|
| **描述** | 同 FP-2.2，应用于报表6（数据区 C6~K15，total_row=16） |
| **输入** | 模板 worksheet（报表6） + 权属数据 + 报表6配置 |
| **输出** | 数据区数值被填入 |
| **依赖** | FP-2.1 |
| **验证** | 对比数值一致性 |

### FP-2.6 报表3写入（资产运营情况表，特殊排列）

| 项 | 内容 |
|---|---|
| **描述** | 与 FP-2.2 逻辑相同，但 row_mapping 中侨乡文体在第14行（古城集团之后），验证匹配逻辑是否天然兼容 |
| **输入** | 模板 worksheet（报表3） + 权属数据 + 报表3配置 |
| **输出** | 数据区数值被填入，侨乡文体数据写入第14行 |
| **依赖** | FP-2.1 |
| **验证** | 确认侨乡文体数据正确写入汇总表第14行（而非第9行），且古城集团在第13行 |

**关键验证点**：因为匹配逻辑是按 B 列名称查找，不依赖行号顺序，所以报表3只需在配置中把侨乡文体的行号写为14即可，核心代码无需改动。验证这一点是本功能点的核心。

---

## 第三层：特殊结构与保护

### FP-3.1 合并单元格保护

| 项 | 内容 |
|---|---|
| **描述** | 在 `writer.py` 中实现合并单元格保护逻辑：写入前记录合并区域数量，写入时跳过合并区域的非左上角单元格，写入后验证合并区域未被破坏 |
| **输入** | 模板 worksheet |
| **输出** | 写入后的 worksheet，合并单元格结构完整 |
| **依赖** | FP-2.2（需在报表1写入后验证） |
| **验证** | 写入前后对比报表1的合并单元格数量（应为41处），报表3（14处），报表6（14处） |

**实现细节**：

- 写入前：`len(ws.merged_cells.ranges)` 记录数量
- 写入时：对每个待写入单元格，检查是否属于某个合并区域且非左上角，若是则跳过
- 写入后：再次对比合并区域数量，不一致则告警

### FP-3.2 报表5双栏追加（资产改造情况表）

| 项 | 内容 |
|---|---|
| **描述** | 实现 `report5_handler.py`：清空数据区 → 从各权属文件提取报表5左右两侧明细 → 追加到汇总表 |
| **输入** | 模板 worksheet（报表5） + 所有权属文件的报表5数据 + 报表5配置 |
| **输出** | 汇总表报表5中，左侧（A~E列）和右侧（F~J列）分别包含全部权属的改造项目明细 |
| **依赖** | FP-1.3 |
| **验证** | 统计左右两侧追加的总行数，与各权属文件报表5的明细行数之和对比，确认一致；检查序号是否连续 |

**处理逻辑**：

1. 清空左侧 A5~E63、右侧 F5~J63 的数据区（保留合计行公式）
2. 遍历所有权属文件，对每个权属：
   - 读取其报表5左侧（A~E列）从第5行到合计行之前的所有明细行
   - 读取其报表5右侧（F~J列）从第5行到合计行之前的所有明细行
3. 将左侧明细追加到汇总表左侧，重新编序号
4. 将右侧明细追加到汇总表右侧，重新编序号
5. 超出模板预置行数时给出告警

### FP-3.3 报表7多子表园区匹配

| 项 | 内容 |
|---|---|
| **描述** | 实现 `report7_handler.py`：遍历4个子表 → 按园区名称（A列）匹配 → 写入数据 |
| **输入** | 模板 worksheet（报表7） + 所有权属文件的报表7数据 + 报表7配置 |
| **输出** | 汇总表报表7中，4个子表的园区数据被填入 |
| **依赖** | FP-1.3、FP-3.1 |
| **验证** | 对比每个园区的数据与对应权属文件中的数值是否一致 |

**处理逻辑**：
1. 遍历 `sub_tables` 配置，对每个子表：
   - 遍历 `row_mapping`（行号→园区名称）
   - 在权属文件中按 A 列查找园区名称，找到对应数据行
   - 将该行数据写入汇总表对应行（跳过 total_row）
2. 园区可能属于不同权属（如侨乡文体的园区在中侨集团文件中，酒管集团的园区在泉旅酒管文件中），需通过 `ownership_files` 配置中的 units 列表反查

**园区→权属反查**：配置文件需额外声明园区归属关系，或通过所有权属文件轮询查找。

### FP-3.4 报表8明细追加（非自有资产情况）

| 项 | 内容 |
|---|---|
| **描述** | 实现 `report8_handler.py`：清空数据区 → 从各权属文件提取报表8明细 → 追加到汇总表并重新编号 |
| **输入** | 模板 worksheet（报表8） + 所有权属文件的报表8数据 + 报表8配置 |
| **输出** | 汇总表报表8中，包含全部权属的非自有资产明细（重新编号） |
| **依赖** | FP-1.3 |
| **验证** | 统计追加的总行数，与各权属文件报表8的明细行数之和对比；检查序号从1开始连续 |

**处理逻辑**：
1. 清空 A5~L155 数据区
2. 遍历所有权属文件，读取其报表8从第5行到末行的所有明细行（识别空行终止）
3. 追加到汇总表，A列重新编号，B~L列照搬数据
4. 超出模板预置行数时给出告警

---

## 第四层：数据校验

### FP-4.1 合计值校验

| 项 | 内容 |
|---|---|
| **描述** | 实现 `validator.py` 中的 `validate_totals()`：对报表1/2/3/4/6，加总各权属写入的数值，与模板合计行（公式计算值）对比 |
| **输入** | 已写入的汇总表 workbook + 各报表配置 |
| **输出** | 校验报告（每个报表：通过/偏差值/告警） |
| **依赖** | FP-2.2~2.6（需在固定行数报表写入完成后执行） |
| **验证** | 人为制造一个数值偏差，确认校验能检测到并报告 |

**校验逻辑**：
- 对每个报表的 data_start_col 到 data_end_col 的每一列：
  - 遍历 data_start_row 到 data_end_row 所有行，累加数值
  - 与 total_row 对应列的公式计算值对比
  - 偏差超过 0.01 时标记

### FP-4.2 空值/异常值检测

| 项 | 内容 |
|---|---|
| **描述** | 实现 `validator.py` 中的 `detect_anomalies()`：检测所有写入后的数据区，标记异常值 |
| **输入** | 已写入的汇总表 workbook |
| **输出** | 异常报告（含位置、值、异常类型） |
| **依赖** | FP-2.2~2.6、FP-3.2~3.4 |
| **验证** | 人为在某单元格写入负值，确认检测到 |

**检测项**：
- 负值：面积类字段（㎡）为负数
- 异常大值：单值超过合理阈值（如 1,000,000 ㎡）
- 关键字段为空：权属行中数据列全部为 None（可能漏填）

---

## 第五层：输出

### FP-5.1 汇总文件保存

| 项 | 内容 |
|---|---|
| **描述** | 保存汇总后的 workbook，文件名包含季度标识 |
| **输入** | 已写入的汇总表 workbook + 季度参数 |
| **输出** | `output/文旅集团{季度}资产运营情况表（汇总）.xlsx` |
| **依赖** | 所有写入功能点 |
| **验证** | 文件成功生成，能用 Excel 正常打开，格式与模板一致 |

### FP-5.2 处理日志输出

| 项 | 内容 |
|---|---|
| **描述** | 汇总完成后输出完整处理日志，包含各权属读取状态、各报表写入行数、校验结果、异常告警 |
| **输入** | 整个流程的执行状态 |
| **输出** | 日志文件（`output/` 目录下），内容格式见需求 NFR-3.1 |
| **依赖** | FP-0.3、所有写入和校验功能点 |
| **验证** | 查看日志文件，确认包含所有关键信息且格式清晰 |

**日志必须包含**：
- 每个权属文件的读取状态（成功/失败/跳过）
- 每个报表的写入行数（如 "报表1：写入 9 行"）
- 校验结果（通过/告警及具体偏差值）
- 异常告警（缺失工作表、未找到单位、空值等）

---

## 第六层：集成

### FP-6.1 主流程编排

| 项 | 内容 |
|---|---|
| **描述** | 实现 `main.py`，串联所有模块：加载配置 → 加载模板 → 校验模板 → 加载权属数据 → 逐报表写入 → 校验 → 保存 → 输出日志 |
| **输入** | config.yaml + 模板文件 + 权属数据目录 |
| **输出** | 汇总文件 + 日志文件 |
| **依赖** | 所有功能点 |
| **验证** | 端到端运行，输出完整的汇总文件，人工抽查5个以上数据点 |

### FP-6.2 错误处理与优雅降级

| 项 | 内容 |
|---|---|
| **描述** | 实现异常处理机制：权属文件缺失某工作表时跳过该报表并告警；权属文件中某单位数据为空时跳过该行并记录；整体流程不因单个错误中断 |
| **输入** | 各种异常场景（见验证方式） |
| **输出** | 日志中明确记录异常，但流程继续执行完 |
| **依赖** | FP-6.1 |
| **验证** | 模拟以下场景：(1) 删除某个权属文件的报表5工作表 (2) 权属文件中某单位数据全部为空 (3) 数据目录下缺少一个权属文件。确认均告警但不中断 |

### FP-6.3 选择性执行

| 项 | 内容 |
|---|---|
| **描述** | 支持 `reports_to_run` 配置，只处理指定的报表 |
| **输入** | `config.runtime.reports_to_run: [1, 2, 3, 4]` |
| **输出** | 仅汇总报表1~4，报表5~8保持模板原样 |
| **依赖** | FP-6.1 |
| **验证** | 设置 `reports_to_run: [1, 4]`，确认只有报表1和报表4被写入，其余报表数据区为空 |

---

## 功能点清单汇总

| 编号 | 名称 | 层级 | 依赖 | 可独立验证 |
|:---:|------|:---:|------|:---:|
| FP-0.1 | 项目骨架搭建 | 基础设施 | — | ✓ |
| FP-0.2 | 配置加载 | 基础设施 | FP-0.1 | ✓ |
| FP-0.3 | 日志模块 | 基础设施 | FP-0.2 | ✓ |
| FP-1.1 | 模板加载 | 数据读取 | FP-0.2 | ✓ |
| FP-1.2 | 模板一致性校验 | 数据读取 | FP-1.1 | ✓ |
| FP-1.3 | 权属文件识别与加载 | 数据读取 | FP-0.2 | ✓ |
| FP-2.1 | B列单位名称匹配器 | 核心匹配 | FP-1.3 | ✓ |
| FP-2.2 | 报表1写入 | 固定行数报表 | FP-2.1 | ✓ |
| FP-2.3 | 报表2写入 | 固定行数报表 | FP-2.1 | ✓ |
| FP-2.4 | 报表4写入 | 固定行数报表 | FP-2.1 | ✓ |
| FP-2.5 | 报表6写入 | 固定行数报表 | FP-2.1 | ✓ |
| FP-2.6 | 报表3写入（特殊排列） | 固定行数报表 | FP-2.1 | ✓ |
| FP-3.1 | 合并单元格保护 | 特殊结构 | FP-2.2 | ✓ |
| FP-3.2 | 报表5双栏追加 | 特殊结构 | FP-1.3 | ✓ |
| FP-3.3 | 报表7多子表园区匹配 | 特殊结构 | FP-1.3, FP-3.1 | ✓ |
| FP-3.4 | 报表8明细追加 | 特殊结构 | FP-1.3 | ✓ |
| FP-4.1 | 合计值校验 | 数据校验 | FP-2.2~2.6 | ✓ |
| FP-4.2 | 空值/异常值检测 | 数据校验 | FP-2.2~2.6, FP-3.2~3.4 | ✓ |
| FP-5.1 | 汇总文件保存 | 输出 | 所有写入FP | ✓ |
| FP-5.2 | 处理日志输出 | 输出 | FP-0.3, 所有FP | ✓ |
| FP-6.1 | 主流程编排 | 集成 | 所有FP | — |
| FP-6.2 | 错误处理与优雅降级 | 集成 | FP-6.1 | ✓ |
| FP-6.3 | 选择性执行 | 集成 | FP-6.1 | ✓ |

**共计 23 个功能点**，从基础设施到集成，按依赖顺序逐层实现。

---



