# 后续改进

## 核心功能

### 差异识别

核心目标：

识别模板与权属数据，同比、环比数据的修改，并打印专项的提示信息。如：表3中，商业集团的J列：“2026年第一季度租金（应）收入（元）”，模板与权属表的值不一致，即有修改。识别出该类修改，打印相应的提示信息。

修改方案：

1. 明确检测范围=》修改配置文件：
   - 表1：C、E；
   - 表3：D、G、J。
2. 检测时机：需要在写入数据前，否则模板数据将被覆盖。
3. 差异分类：仅需判别不同。
4. 提示信息：每项差异输出一条完整、可追溯的信息。

修改结果：

1. 配置文件加入新字段：`difference_check_columns`；
2. 检测已置于写入前：模板值尚未覆盖时，与来源值直接判断是否不同；不同即输出 `WARNING` 专项提示，随后仍按原逻辑写入权属值。
3. 每条提示包含报表、权属、单位、字段名、列号、模板单元格和值、来源文件/单元格和值，便于追溯。

---



### ==字体统一==

当前效果还可以，不作多余修改。

---



### 批注处理

核心功能：

保留权属批注，同时在日志中打印，方便查看。

问题与改进：

模板原有批注

模板的原有批注，多是先前遗留的批注，继续保留会有歧义，如表7的S6。=》在写入数据前，**清除模板文件的批注**，并在日志中打印提示信息。

批注作者规范

规范写入批注的作者名称，将批注作者名称改为对应权属，如admin改为古城集团。

=》批注上显示的作者，实际是批注内容的一部分，而非真正的作者属性，所以该项修改并未实际生效。为了避免代码过于臃肿，且该项功能非必须。故撤销该项改进。

修改结果：

- 清除模板遗留批注；
- 复制权属批注到汇总表；
- 批注作者和正文均按来源原样保留。

---



### 复用

配置文件：季度信息=》从配置文件中去除，改为参数传入。

模板文件位置：=》改为可选参数，默认使用配置文件中指定的文件路径。



## 鲁棒性

### 顺序调整

条目顺序的调整

估计无影响

---



### 集团名称匹配

核心目标：

全称与简称匹配

修改结果：

根据配置文件精准匹配

---



### 权属增加与减少

处理方案：

“只发现、不自动处理”

- 增加：打印警告，不写入；
- 减少：跳过该行，打印警告。

修改明细：

- 固定报表1、2、3、4、6在写入前**检测配置外条目**。
- 配置外条目**只打印警告，不写入**。

---



### 缺失值处理

写入权属数据时，仅写入数值，其余为空。

对“关键数据列全部为空”的行，跳过不影响程序进行，同时发出警告。

经过对业务的进一步熟悉（比较一季度表格），将以上情形的处理方式，修改为：

- 对于数值列，将非数值（空、斜杠等）写入为0；
- 对于公式列，保留模板的公式。

对于**改写为0后，公式报错**的情况：修改excel公式为`=IFERROR(F6/C6,"/")`，将错误显示为`/`。

发现问题：表1的AG列是备注，由于备注为空，所以被填入数值0。但这不合逻辑，应该依旧为空。所以，以上写入为0的处理不应该包含文本列。

修改结果：

- 固定报表（1、2、3、4、6）中，非公式、非文本列仅保留数值；空值、`/`、其他文本均写为 `0`。

- 配置文件：`text_columns` 中的列直接复制来源值，空备注保持空。报表1 AG 已列为文本列，并已从 `area_columns` 移除。

- 公式列只作识别，不再写入公式；模板公式内容保持不变。报表3为 L、M，报表4为 H、I、J。

- 报表5、7、8未受到本次规则影响。

---

### ==缺失行处理==

在关键数据列全为0时，改为”/"。



### 公式检查

依赖模板

#### 公式列的处理

- 对于固定报表，保留原有公式；
- 报表5：明细行数变化=》自动调整行数，并加上合计行；
- 报表7：原模版无公式，直接写入权属数据，不区分数值列和公式列。
- 报表8无公式。

修改方案：

表7也存在模板公式，包括列公式以及汇总行，需要像固定报表一样处理，即跳过公式列的写入。

- `J7`、`F25` 等非标准公式，应当转为数值写入，因为它不是公式列，而是数值列。修正表7的模板，N、O、R、T为公式列。
- 然后侨乡文体公司所管理园区和酒管集团所管理园区两张子表有汇总行；其它子表是只有一个条目，所以没有汇总行。

修改结果：

- `N、O、R、T` 按配置跳过写入，保留模板公式。
- `F、J` 等其他列即使目标原来是公式，也会被来源的已保存数值覆盖。
- 酒管集团汇总行配置为第26行；侨乡文体汇总行仍为第8行。



#### 汇总行的处理

汇总行保留模板的公式，但有错误会被合计校验模块捕捉，需根据警告检查并修改模板。

---



### 合并单元格的处理

保留原合并单元格格式。

---



## 体验优化

### 打印信息的优化

日志分级：控制台打印关键信息，详细内容查询日志；

格式：体现处理步骤；

语言：简洁明了；

风格：避免主观判断，仅客观事实表述。

---



### 结果文件路径

文件已存在时，覆盖；无法覆盖，则添加时间戳后缀。

---



## =》完整流程梳理+说明文档

已完成



## =》项目结构优化





## =》从汇总结果再生成下季度模板表，形成闭环。

输入当前季度最终汇总表，更新表名、列名、同比/环比等信息，清空需要更新的数据等，生成下季度的汇总表。



1. 清除填充等样式
2. 公式补全
3. 表5、表8行数回缩
4. 表7公式需修正
5. 合并单元格：有点问题，需要确定边界，先搁置该项优化。



汇总表格式修改=》模板如何自动修改？当前需手动修改



=>能否根据表格生成配置文件？



## =》上传github











